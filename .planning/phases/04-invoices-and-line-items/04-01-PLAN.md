---
phase: 04-invoices-and-line-items
plan: 01
title: Invoices and Line Items Migrations
type: implementation
wave: 1
depends_on: []
files:
  - minidodo-infra/src/postgres/migrations/0004_create_invoices.up.sql
  - minidodo-infra/src/postgres/migrations/0004_create_invoices.down.sql
  - minidodo-infra/src/postgres/migrations/0005_create_line_items.up.sql
  - minidodo-infra/src/postgres/migrations/0005_create_line_items.down.sql
---

# Plan 04-01: Invoices and Line Items Migrations

## Goal
Create separate PostgreSQL migrations for `invoices` (`0004_create_invoices`) and `line_items` (`0005_create_line_items`) using lowercase SQL, integer minor units (`bigint` for cents), UUID v4 primary keys, state checks, and notable indexes as specified in DESIGN.md §1.

## Proposed Changes

### 1. `minidodo-infra/src/postgres/migrations/0004_create_invoices.up.sql`
- Table `invoices`:
  - `id uuid primary key default gen_random_uuid()`
  - `business_id uuid not null references businesses(id) on delete cascade`
  - `customer_id uuid not null references customers(id) on delete restrict`
  - `state varchar(32) not null check (state in ('draft', 'open', 'processing', 'paid', 'void', 'uncollectible'))`
  - `total_cents bigint not null`
  - `due_date date not null`
  - `created_at timestamptz not null default current_timestamp`
- Indexes:
  - `idx_invoices_business_id_state` on `invoices(business_id, state)`
  - `idx_invoices_customer_id` on `invoices(customer_id)`
  - `idx_invoices_business_id_created_at` on `invoices(business_id, created_at desc)`

### 2. `minidodo-infra/src/postgres/migrations/0004_create_invoices.down.sql`
- `drop table if exists invoices cascade;`

### 3. `minidodo-infra/src/postgres/migrations/0005_create_line_items.up.sql`
- Table `line_items`:
  - `id uuid primary key default gen_random_uuid()`
  - `invoice_id uuid not null references invoices(id) on delete cascade`
  - `description text not null`
  - `quantity int not null check (quantity > 0)`
  - `unit_amount_cents bigint not null check (unit_amount_cents >= 0)`
  - `created_at timestamptz not null default current_timestamp`
- Indexes:
  - `idx_line_items_invoice_id` on `line_items(invoice_id)`

### 4. `minidodo-infra/src/postgres/migrations/0005_create_line_items.down.sql`
- `drop table if exists line_items cascade;`

## Verification
- Run migrations via `cargo run -p minidodo-cli -- migrate`.
- Verify tables and foreign key constraints in Postgres.
